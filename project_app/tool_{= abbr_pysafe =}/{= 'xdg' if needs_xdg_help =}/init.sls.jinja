# vim: ft=sls

{#-
    Ensures {= service_name =} adheres to the XDG spec
    as best as possible for all managed users.
    Has a dependency on `tool_{= abbr_pysafe =}.package`_.
#}

include:
  - .migrated
