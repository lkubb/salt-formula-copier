# frozen_string_literal: true

control '{= abbr_pysafe =}.package.install' do
  title 'The required package should be installed'

  package_name = '{= pkg =}'

  describe package(package_name) do
    it { should be_installed }
  end
end
