# vim: ft=sls

{#-
    Configures the {= abbr_pysafe =} {= subcomponent =} subcomponent{! if config !} and has a
    dependency on `{= abbr_pysafe =}.config`_ via include list{! endif !}.
#}
include:
  - .file
